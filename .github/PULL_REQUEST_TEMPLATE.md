## Summary

<!-- Why this change exists. Link issue if any. -->

-

## Test plan

- [ ] `go test ./... -count=1 -race`
- [ ] `golangci-lint run ./...` (if installed)
- [ ] `go build -o airlock ./cmd/airlock`
- [ ] Manual CLI check (command + expected result):

```bash
# …
```
